# Vitesse by the numbers

A single-page, data-led profile of Vitesse Arnhem, prepared by ClubOS.

The site is a static `index.html` served with GitHub Pages. There is no build step: edit `index.html` and push to `main`.

Fonts are loaded from Google Fonts; everything else is inline.
